# Results

Store generated outputs here:

- metrics
- diagnostic plots
- posterior predictive checks
- short result summaries

Each non-trivial run should save enough information to reproduce the simulator configuration and interpretation.

Posterior-diagnostic runs belong under `posterior_diagnostics/<variant>/` and
should contain the evaluation arrays, copied run configuration, metric tables,
plots, and a short interpretation.
