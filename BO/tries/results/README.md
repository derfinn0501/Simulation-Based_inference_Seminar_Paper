# Results

This folder is reserved for future consolidated result exports.

Current active experiment result bundles still live in:

```text
experiments/results/
```

Use this root `results/` folder when the project decides to separate reusable
experiment code from final reported outputs.
