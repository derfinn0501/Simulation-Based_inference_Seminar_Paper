# prior_mean

`prior_mean` is the trivial baseline that always predicts the mean of the prior for every observation.

It is not a useful posterior estimator. It is included to answer:

```text
Does any method beat doing no inference?
```

Budget notes:

- See `budget/README.md`.
