# rectified_fmpe

`rectified_fmpe` is the current lightweight FMPE-style posterior estimator.

It learns a conditional vector field from Gaussian noise to posterior parameter samples:

```text
v_phi(t, theta_t, x, psi)
```

Posterior samples are generated by integrating this vector field.

Budget notes:

- See `budget/README.md`.
